// ⚠️ 이 파일은 견본이다. 새 개발 환경을 세팅할 때 아래 이름으로 복사해서 쓴다:
//
//     cp src/constants/holidayApiKey.example.ts src/constants/holidayApiKey.ts
//
// 그런 다음 HOLIDAY_API_KEY에 실제 서비스키를 붙여넣으면 된다.
//
// ── 왜 실제 키는 git에 없나 ──────────────────────────────────────────
// 이 저장소는 공개(public)다. docs/privacy.html이 GitHub Pages로 서비스되고 있고
// 그 주소가 Play Console에 등록한 개인정보처리방침 URL이라, 저장소를 비공개로 돌리면
// 그 페이지가 내려가 심사에 문제가 생긴다. 그래서 공개를 유지하고 키만 빼둔다
// (공개 저장소의 API 키는 자동 수집 봇의 표적이 된다).
//
// ── 키가 없으면 어떻게 되나 ──────────────────────────────────────────
// 빌드와 앱 실행은 정상이다. 공휴일 자동 갱신만 건너뛴다(useHolidaySync가
// 빈 키면 즉시 return). 달력에는 내장 공휴일 표(constants/holidays.ts, 2027년까지)가
// 그대로 표시되므로 화면이 비지 않는다.
//
// 다만 배포용 빌드는 반드시 실제 키를 넣고 만들 것 — 키가 있어야 새로 지정되는
// 임시공휴일·대체공휴일이 자동 반영된다.
//
// ── 키 발급 ─────────────────────────────────────────────────────────
// https://www.data.go.kr → "한국천문연구원_특일 정보" 활용신청 (무료)
// 발급 후 "일반 인증키(Decoding)" 값을 그대로 넣는다.
export const HOLIDAY_API_KEY = '';
